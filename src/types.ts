export interface Input {
  name: string;
  value: number;
  solved: boolean;
  custom: boolean;
}

export interface Variable {
  name: string;
  value: number;
  solved: boolean;
  custom: boolean;
}

